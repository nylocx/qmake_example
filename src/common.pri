# Configurations that are shared between all projects

# output directories of build artifacts
UI_DIR = .uic
MOC_DIR = .moc
OBJECTS_DIR = .obj
RCC_DIR = .rcc

# Always install the project result dll or executable
INSTALLS += target
